# 验证码邮件：免费额度接入与发布检查

## 已配置的云资源

2026-09-03：腾讯云 SES 中国香港区域（`ap-hongkong`），`mail.chitouen.cn` 四项 DNS 验证通过，发件人 `ChiTouEN II <noreply@mail.chitouen.cn>`。验证码纯文本模板 ID `215390`，变量为 `{{code}}`；提交时状态为审核中，启用前必须重新确认审核通过。正文包含中英文说明、10 分钟有效期及非本人操作提示。

没有开通独立 IP；网站根域名 A 记录不变。云资源配置不代表应用已上线发信。

## 免费额度边界

腾讯云赠送的是**整个账号累计 1,000 封**，不是每月重置，超出自动按量收费。[官方计费说明](https://cloud.tencent.cn/document/product/1288/47930)

本项目用数据库 `email_send_budget` 保存累计发送上限 `send_limit` 和已预留名额 `reserved_count`。默认上限 **0**，未核实账号实际剩余免费额度前不能启用。每次真实 SES/SMTP 发送前原子预留一个名额，多实例共享；失败、超时也不返还，避免已经投递却重试超额。没有自动重试或 SES 失败后的 SMTP 补发。本地模拟邮件不占用名额。

这是应用级保护，不是腾讯云账单硬上限。其他项目、控制台手动发送、其他密钥或恢复旧数据库备份，都可能绕过/影响累计额度。该免费阶段应将账号的邮件发送独占给本项目，控制台测试也需纳入核算；若无法保证，应保持停发，不能承诺不会收费。

## 正式启用前（本次未执行）

1. 确认模板审核通过，核实腾讯云账号**全部区域**的实际已用/剩余额度及是否有其他发送渠道。不要把“赠送 1,000 封”当成“剩余 1,000 封”。
2. 创建专用最小权限凭证（仅需 `ses:SendEmail`），只存服务器 `.env.production`；不进入 Git、日志、聊天或 `NEXT_PUBLIC_*`。填写示例文件中的 SES_REGION、SES_TEMPLATE_ID、SES_FROM_EMAIL 和凭证，保持 `EMAIL_LOGIN_ENABLED=false`，不要设置 `LOCAL_AUTH_MAIL=1`。
3. 备份数据库，人工应用 `deploy/postgres/011-email-send-budget.sql`。已有 Docker 数据卷不会自动重跑初始化 SQL；脚本可重复执行，不重置计数。应用不会在运行时自动建表或增加额度，缺表/缺行/数据库故障均拒绝发送。
4. 查询 `select send_limit, reserved_count from email_send_budget where id = 1;`。人工将**累计上限**设为 `min(1000, 当前 reserved_count + 已核实的剩余免费额度 - 安全余量)`，建议留 50 封余量；结果不足则继续停发。仅修改 `send_limit`，绝不重置 `reserved_count`。当前尚未核实剩余额度，不预设 950 或 1000。
5. 自动化通过且用户批准发布后，先限制外部访问进行真实 Gmail 收件与登录测试，测试同样消耗额度；确认成功后再开放公众登录。供应商接受请求不代表进入收件箱，应由测试邮箱确认实际到达。

## 停发与恢复

- 将数据库 `send_limit` 设为 0，阻止后续预留；已预留/在途邮件可能继续完成。关闭 `EMAIL_LOGIN_ENABLED` 并重启应用，使登录入口隐藏；停用专用密钥可阻止凭证继续使用。
- 不删除计数表，不清零计数，不以每天/月自动重置，不通过切换 SMTP 绕过限额。
- 恢复数据库备份或改用新数据库时，先保持停发，重新核实云端总用量再决定预算。
- 停用邮件不会影响做题网站访问，但无法接收新的登录验证码。停发不抵消此前已经产生的费用。

## 本地验收

`npm run local:db` 后运行 `node tests/mailer.mjs`，或停止开发服务后运行 `npm run test:local`。

邮件测试将 SES、SMTP 替换为模拟实现，只连接已验证数据目录的本地数据库，在本次创建的隔离 schema 内运行并清理。覆盖模板参数、配置缺失、20 请求争抢 5 个名额、重复迁移/模块重载不清零、超时不退额度/不重试、数据库错误、SMTP 共享限额及本地邮件隔离。不会读取真实密钥或调用腾讯云发信。
