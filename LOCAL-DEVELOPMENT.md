# 本地 + 正式环境

本地修改、自动化测试和体验验收完成后，再明确确认发布到正式环境。不设公网测试站。

## 本地使用

这台 Mac 已安装 PostgreSQL 工具（initdb、pg_ctl）。其他电脑需先安装这些工具和项目依赖。

- `npm run dev`：自动准备独立数据库，打开 http://127.0.0.1:3000 。
- 停止开发服务后，`npm run test:local`：启动临时本地网页服务，运行页面检查及做题核心流程测试，然后停止网页服务。
- `npm run verify`：代码检查及正式构建检查；不发布。
- `npm run local:stop`：停止本项目本地数据库，不删除数据。先停止本地网页服务。

数据库只监听本机 55432 端口，数据保存在忽略提交的 `.local-db/`。初始化只导入仓库内题目与答案，不复制正式用户。重复启动保留本地记录；测试也会保留匿名测试记录。不自动清空数据库。

本地启动固定覆盖数据库连接和登录密钥，并将真实邮件、AI 凭证置空，不改动已有 `.env` 文件。AI 查词、生成和邮件发送需后续单独联调，此处不计入通过的测试。不要绕过本地启动脚本运行 `next dev`，不要在本地转发端口接入正式数据库。

## 每次发布前

1. 停止本地开发服务，运行 `npm run verify` 和 `npm run test:local`。
2. 在本地人工验收本次改动。当前自动化覆盖页面打开、答题保存、提交评分、精读解锁、Redo 与刷新恢复，不覆盖动画、划词手势、邮件和真实 AI。
3. 用户明确确认后，才提交、推送、部署；部署前备份正式数据并保留上一版本。
4. 部署后运行只读 smoke 检查和必要人工验收。核心写入测试禁止指向公网。

现有 GitHub CI 运行 lint/build；本地核心流程测试仍需执行，不代表 CI 已全覆盖。

## 邮箱登录本地验收

`npm run dev` 仅在隔离的开发数据库下启用本地模拟邮件。验证码保存在 `.local-db/mail.jsonl`，不会发往真实邮箱，也不会通过 HTTP 接口返回；该文件包含本地测试验证码，不应分享或提交。

`npm run test:local` 还会使用随机的 `example.invalid` 邮箱测试验证码错误/过期/重复使用/连续输错、游客记录迁移、跨设备保持登录、账号隔离及退出登录。测试只修改本次创建的验证码时间，不修改正式数据。真实投递、浏览器操作与视觉效果仍需另行验收。

当前同步范围：答案、成绩、做题历史和精读解锁状态。划词高亮、AI 解析仍留在浏览器本地。

正式环境默认关闭登录（`EMAIL_LOGIN_ENABLED=false`）。邮件服务配置、真实收件及安全验收完成，并经用户确认后，才能改为 `true`。正式环境不得设置 `LOCAL_AUTH_MAIL=1`，模拟邮件模式在正式运行时会拒绝发送。

邮件模板配置、免费额度限制与停发流程见 [邮件接入检查](EMAIL-SETUP.md)。`npm run test:local` 包含 `tests/mailer.mjs`：模拟邮件服务并使用隔离数据库测试并发额度保护，不消耗真实邮件额度。真实投递仍需审核通过后单独验收。
